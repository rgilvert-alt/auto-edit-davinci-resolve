# Core (always needed)
typer>=0.12
python-dotenv>=1.0
numpy>=1.24

# Analyzer backends (imported lazily; install via: pip install -e '.[analyzers]')
# Note: librosa may fail to build on Python 3.13 (llvmlite); beat snap has an ffmpeg+numpy fallback.
faster-whisper>=1.0
scenedetect[opencv]>=0.6.4
librosa>=0.10
soundfile>=0.12
onnxruntime>=1.20
tokenizers>=0.20

# Our MCP server
fastmcp>=2.0

# Desktop UI
flet>=0.25

# Dev / tests
pytest>=8.0

# System deps (not pip): ffmpeg on PATH, DaVinci Resolve Studio scripting API.
